                          </div>
                      ))}
                  </div>
              </div>
          )}

                                        {/* Installation Guide */}
          <div className="w-full pt-8" id="download">
<div className="bg-slate-50 dark:bg-[#0F172A] rounded-2xl border border-slate-200 dark:border-slate-800 p-6 sm:p-8 flex flex-col h-full shadow-lg">
                  <div className="flex items-center gap-4 mb-8">
                      <div className="w-12 h-12 bg-primary-500/10 text-primary-500 rounded-xl flex items-center justify-center">
                          <Icon name="Box" size={24} />
                      </div>
                      <div>
                          <h3 className="text-xl font-bold text-slate-900 dark:text-white">Installation Guide</h3>
                          <p className="text-sm text-slate-500 dark:text-slate-400">Step-by-step setup process</p>
                      </div>
                  </div>

                  
                  {item.installSteps && item.installSteps.length > 0 ? (
                      <div className="space-y-6 flex-1">
                          {item.installSteps.map((step, idx) => (
                              <div key={idx} className="flex gap-4">
                                  <div className="w-8 h-8 rounded-full border border-primary-500/30 flex items-center justify-center text-primary-500 font-bold text-sm shrink-0">
                                      {idx + 1}
                                  </div>
                                  <p className="text-sm text-slate-700 dark:text-slate-300 pt-1 leading-relaxed">
                                      {step}
                                  </p>
                              </div>
                          ))}
                      </div>
                  ) : (
                      <div className="space-y-6 flex-1 flex flex-col items-center justify-center text-slate-500 dark:text-slate-400 py-8">
                          <Icon name="Box" size={48} className="opacity-20 mb-4" />
                          <p className="font-bold">No specific installation steps provided.</p>
                          <p className="text-xs mt-1 text-center">Usually, just run the setup.exe or play from the extracted folder.</p>
                      </div>
                  )}

                  
              </div>
          </div>
          
          {/* System Requirements */}
          <div className="w-full pt-8">
<div className="bg-slate-50 dark:bg-[#0F172A] rounded-2xl border border-slate-200 dark:border-slate-800 p-6 sm:p-8 flex-1 shadow-lg">
                      <div className="flex items-center gap-4 mb-8">
                          <div className="w-12 h-12 bg-purple-500/10 text-purple-500 rounded-xl flex items-center justify-center">
                          <Icon name="Cpu" size={24} />
                      </div>
                      <div>
                          <h3 className="text-xl font-bold text-slate-900 dark:text-white">System Requirements</h3>
                          <p className="text-sm text-slate-500 dark:text-slate-400">Minimum specifications needed</p>
                      </div>
                  </div>

                  <HardwareCompatibility requirements={item.systemReqs} globalSpecs={globalSpecs} />
<div className="mb-8"></div>

                  <div className="text-center text-xs font-bold uppercase tracking-widest text-slate-500 dark:text-slate-400 mb-6 border-b border-slate-200 dark:border-slate-800 pb-4">
                      REQUIRES A 64-BIT PROCESSOR AND OPERATING SYSTEM
                  </div>

                  <div className="space-y-4">
                      {item.systemReqs && item.systemReqs.map((req, idx) => (
                          <div key={idx} className="flex justify-between items-center text-sm border-b border-slate-200/50 dark:border-slate-800/50 pb-3 last:border-0 last:pb-0">
                              <span className="text-slate-500 dark:text-slate-400 font-medium flex items-center gap-2">
                                  {req.label.toLowerCase().includes('os') && <Icon name="Windows" size={16} />}
                                  {req.label.toLowerCase().includes('processor') && <Icon name="Cpu" size={16} />}
                                  {req.label.toLowerCase().includes('memory') && <Icon name="RAM" size={16} />}
                                  {req.label.toLowerCase().includes('graphics') && <Icon name="GPU" size={16} />}
                                  {req.label.toLowerCase().includes('storage') && <Icon name="Storage" size={16} />}
                                  {req.label}
                              </span>
                              <span className="text-slate-900 dark:text-slate-200 font-bold text-right max-w-[60%]">{req.value}</span>
                          </div>
                      ))}
                  </div>
                  <div className="bg-slate-50 dark:bg-[#0F172A] rounded-2xl border border-slate-200 dark:border-slate-800 p-6 sm:p-8 h-fit shadow-lg">
             <div className="flex items-center gap-4 mb-6">
                 <div className="w-10 h-10 bg-orange-500/10 text-orange-500 rounded-xl flex items-center justify-center">
                     <Icon name="Wrench" size={20} />
                 </div>
                 <h3 className="text-xl font-bold text-slate-900 dark:text-white">Tools You Need</h3>
             </div>
             <div className="flex flex-wrap gap-3">
                 {item.toolsNeeded && item.toolsNeeded.map((tool, idx) => (
                     <a key={idx} href={tool.url} target="_blank" rel="noreferrer" className="flex items-center gap-2 px-4 py-2 bg-white dark:bg-slate-800/50 border border-slate-200 dark:border-slate-700 rounded-lg hover:border-primary-500/50 transition-colors">
                         <Icon name="Terminal" size={16} className="text-slate-400" />
                         <span className="text-sm font-bold text-slate-700 dark:text-slate-300">{tool.name}</span>
                     </a>
                 ))}
                 {(!item.toolsNeeded || item.toolsNeeded.length === 0) && (
                     <div className="text-sm text-slate-500 dark:text-slate-400">No specific tools required for this game.</div>
                 )}
              </div>
          </div>

                  </div>
          </div>

          {/* Tools You Need */}
          <div className="w-full pt-8">
<div className="bg-slate-50 dark:bg-[#0F172A] rounded-2xl border border-slate-200 dark:border-slate-800 p-6 sm:p-8 h-fit shadow-lg">
             <div className="flex items-center gap-4 mb-6">
                 <div className="w-10 h-10 bg-orange-500/10 text-orange-500 rounded-xl flex items-center justify-center">
                     <Icon name="Wrench" size={20} />
                 </div>
                 <h3 className="text-xl font-bold text-slate-900 dark:text-white">Tools You Need</h3>
             </div>
             <div className="flex flex-wrap gap-3">
                 {item.toolsNeeded && item.toolsNeeded.map((tool, idx) => (
                     <a key={idx} href={tool.url} target="_blank" rel="noreferrer" className="flex items-center gap-2 px-4 py-2 bg-white dark:bg-slate-800/50 border border-slate-200 dark:border-slate-700 rounded-lg hover:border-primary-500/50 transition-colors">
                         <Icon name="Terminal" size={16} className="text-slate-400" />
                         <span className="text-sm font-bold text-slate-700 dark:text-slate-300">{tool.name}</span>
                     </a>
                 ))}
                 {(!item.toolsNeeded || item.toolsNeeded.length === 0) && (
                     <div className="text-sm text-slate-500 dark:text-slate-400">No specific tools required for this game.</div>
                 )}
              </div>
          </div>
          </div>

          {/* Download Channels */}
          <div className="w-full pt-8">
<div className="bg-slate-50 dark:bg-[#0F172A] rounded-2xl border border-slate-200 dark:border-slate-800 p-6 sm:p-8 shadow-lg">
             <div className="flex items-center gap-4 mb-6">
                 <div className="w-10 h-10 bg-primary-500/10 text-primary-500 rounded-xl flex items-center justify-center">
                     <Icon name="Download" size={20} />
                 </div>
                 <h3 className="text-xl font-bold text-slate-900 dark:text-white">Download Channels</h3>
             </div>
             
             {item.links.full && (
                <div className="mb-6">
                   <h4 className="text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-widest mb-3">Direct Full Download</h4>
                   <DownloadButton 
                        label={`Download Full Game (${item.repackSize || 'Size N/A'})`}
                        sub="Direct Link" 
                        href={item.links.full} 
                        icon="Download" 
                        note={item.links.fullNote || 'You need qBittorrent to download this game via torrent or magnet link. Please ensure it is installed before proceeding.'}
                        onNoteClick={(note) => setNoteModalContent(note)}
                   />
                </div>
             )}

             {item.links.mirrors && item.links.mirrors.length > 0 && (
                <div className="mb-6">
                   <h4 className="text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-widest mb-3">FitGirl Repack Links ({item.links.mirrors.length})</h4>
                   <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
                       {item.links.mirrors.map(part => (
                           <DownloadButton 
                               key={part.id} 
                               label={`Part ${part.id}`} 
                               sub="Download" 
                               href={part.link} 
                               icon="Archive" 
                               secondary 
                               note={part.note}
                               onNoteClick={(note) => setNoteModalContent(note)}
                           />
                       ))}
                   </div>
                </div>
             )}

             {item.links.ankerParts && item.links.ankerParts.length > 0 && (
